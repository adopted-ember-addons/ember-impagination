<div class="demo_inputs">
  <label for="ro">Initial Read Offset:</label>
  {{input value=initialReadOffset type=number id="ro"}}
  <label for="lh">Load Horizon:</label>
  {{input value=loadHorizon type=number id="lh"}}
  <label for="uh">Unload Horizon:</label>
  {{input value=unloadHorizon type=number id="uh"}}
  <label for="ps">Page Size:</label>
  {{input value=pageSize type=number id="ps"}}
</div>
<div class="demo_dataset-wrapper">
  {{#impagination-list fetch=fetch initial-read-offset=initialReadOffset load-horizon=loadHorizon unload-horizon=unloadHorizon page-size=pageSize as |records state pages|}}
    <div class="demo_read-offset">Current Read Offset: {{state.readOffset}} </div>
    <div class="demo_pages">
    {{#each pages as |page index|}}
      {{pages-display  page=page state=state records=records index=index}}
    {{/each}}
    </div>
    <div class="demo_records-wrapper">
      <div class="demo_records-list-container" style="height: 525px; width: 600px;">
        {{#virtual-each
           height=525
           itemHeight=70
           positionIndex=state.readOffset
           items=records as |record actualIndex virtualIndex|
        }}
          <div class="demo_record" style={{color-block record.content.hsl}}>P{{record.page.offset}} - R{{record.index}}</div>
        {{/virtual-each}}
      </div>
    </div>
  {{/impagination-list}}
</div>
